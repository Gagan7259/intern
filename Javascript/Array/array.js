let colors = ['red', 'blue', 'yellow']
//0,1,2 index start with 0
//we can store multiple values in single variable
console.log(colors[1])
colors.push("bluegreen")//insert elements an end of the array
console.log(colors)
colors.pop();//remove the elements of end of the array
console.log(colors)
colors.shift()//remove elemets of start of the array
console.log(colors)
colors.unshift("black")//insert elements of start of an array
console.log(colors)